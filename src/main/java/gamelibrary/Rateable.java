package gamelibrary;

public interface Rateable {
    /**
     * Allows a game to be rated with a specified rating value
     * @param rating the rating assigned to the game
     */
    void rateGame(int rating);
}
